component: http
name: nestDemo

inputs:
  src:
    dist: ./
    hook: npm run build
    exclude:
      - .env
  faas:
    framework: nest
    name: ${name}
  apigw:
    protocols:
      - http
      - https
