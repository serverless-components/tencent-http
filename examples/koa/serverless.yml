app: appDemo
stage: dev

component: http
name: koaDemo

inputs:
  src:
    src: ./
    exclude:
      - .env
  faas:
    framework: koa
    name: ${name}
  apigw:
    protocols:
      - http
      - https
